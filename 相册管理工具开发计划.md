# Android 相册管理工具 — 开发计划

> 目标平台：Android 原生
> 数据来源：读取系统相册/媒体库
> 分享方式：系统分享面板调用
> 后端架构：纯本地，无需后端

---

## 一、项目概览

| 项 | 选型 |
|---|---|
| 语言 | Kotlin 2.0+ |
| UI | Jetpack Compose + Material 3 (Dynamic Color) |
| 架构 | MVVM + Clean Architecture (UI / Domain / Data) |
| DI | Hilt |
| 数据库 | Room（存自定义相册/标签/收藏/隐藏等元数据） |
| 系统相册 | MediaStore API（只索引、不复制原图） |
| 图片加载 | Coil 3 |
| 异步 | Coroutines + Flow |
| 分页 | Paging 3 |
| 导航 | Navigation Compose（单 Activity） |
| 后台任务 | WorkManager（首次扫描、监听媒体变化） |
| 图片识别 | Google ML Kit（人脸 / 图像标签，本地推理） |
| minSdk / target | 26 / 35，编译 SDK 35 |
| 工具链 | AGP 8.7+、Gradle 8.10+、JDK 17 |

**核心设计原则**：原图始终留在系统相册，本 App 只维护一份「元数据 + 索引 + 标注」，删除/移动通过 MediaStore API 执行，卸载 App 时不会丢失用户照片。

**项目定位**：本项目为**个人开源工具**，主要服务作者自己的使用需求，发布到 GitHub 上以 `Apache 2.0` 协议开源（可自由调整）。**大概率不上架应用商店**；即便上架也保持完全免费、无内购、无广告、无追踪。文档中所有涉及「应用商店上架」的内容均为**可选参考**，优先级最低。

### 1.1 命名体系（产品 / 工程一致）

### 1.1 命名体系（产品 / 工程一致）

| 维度 | 命名 | 说明 |
|---|---|---|
| 中文品牌 | **图匣** | 用户可见主名称；store listing 主标 |
| 英文品牌 | **Pixcase** | `Pix` + `Case`，国际化 / 包名 / 衍生标识统一前缀 |
| 商店展示名 | 图匣 - 智能相册管家 | ≤ 30 字符 |
| 副标题（slogan） | 让每一张图像都被找到 | 强调搜索与分类核心卖点 |
| 包名 / `applicationId` | `com.example.pixcase` | 发布前改为你的真实反域名（占位仅供开发） |
| 命名空间 / `namespace` | `com.example.pixcase` | 同上 |
| Application 类 | `PixcaseApp.kt` | `@HiltAndroidApp` |
| 主题样式 | `@style/Theme.Pixcase` | `Material3` |
| Room 数据库文件 | `pixcase.db` | - |
| SharedPreferences 名 | `pixcase_prefs` | - |
| DataStore 名 | `pixcase_settings` | - |
| WorkManager 名称 | `pixcase-work` | - |
| 文件导出默认名 | `pixcase_export_<timestamp>.zip` | - |
| 内部事件埋点前缀 | `pixcase_evt_` | 若未来加入埋点 |
| Logcat tag 前缀 | `Pixcase/` | 便于过滤 |
| GitHub 仓库 | `github.com/<your-username>/pixcase` | 仓库名固定 `pixcase` |
| 包发布坐标 | `io.github.<your-username>:pixcase-android:<version>` | 若后续拆 library 模块用 |
| 开源协议 | `Apache License 2.0` | 详见 § 1.2 |

**替换前全局说明**：本计划中所有 `图匣 Pixcase` / `PixcaseApp` / `com.example.pixcase` 等占位名均已替换为本命名体系；如出现遗漏，以 `Pixcase / 图匣 / com.example.pixcase` 为准。

### 1.2 开源项目治理

> 本项目以**学习 + 个人使用 + 社区分享**为目的，所有面向开源社区的文件（README / 贡献指南 / Issue 模板 / 协议 / 安全策略）从第一个 commit 就建好。

#### 1.2.1 License 选择

| 协议 | 特点 | 推荐度 |
|---|---|---|
| **Apache 2.0** | 允许商用/修改/分发，需保留版权声明 + 变更说明 + NOTICE | ⭐⭐⭐ **本项目采用** |
| MIT | 更宽松，不强制专利授权 | ⭐⭐ |
| GPL-3.0 | 强制衍生作品开源 | ⭐（传染性太强，不适合工具类） |

**仓库根目录强制文件**：

```
LICENSE                # Apache 2.0 全文
NOTICE                 # 第三方归属声明（与 § 2.9 aboutlibraries 联动）
README.md              # 见 § 1.2.2
CHANGELOG.md           # 与 GitHub Releases 同步
CONTRIBUTING.md        # 见 § 1.2.3
CODE_OF_CONDUCT.md     # Contributor Covenant v2.1
SECURITY.md            # 见 § 1.2.5
.github/
├── ISSUE_TEMPLATE/
│   ├── bug_report.yml
│   ├── feature_request.yml
│   └── question.yml
├── PULL_REQUEST_TEMPLATE.md
├── workflows/          # CI / 发布（见 § 3.5）
└── FUNDING.yml         # 可选：赞助按钮
```

#### 1.2.2 README 模板

```markdown
# 图匣 Pixcase

> 让每一张图像都被找到 —— 轻量、本地、强大的 Android 相册管理工具

[徽章：Release] [徽章：API 26+] [徽章：License] [徽章：CI]

## 截图

[截图 1] [截图 2] [截图 3]

## 特性

- 📸 浏览 / 全屏查看 / 手势缩放
- 🗂️ 自定义相册 + 智能相册（截图/自拍/视频/收藏）
- ⚡ Tinder 式快速整理（一键分流到大类）
- 🔍 印象标记 + 大类分类 + 多类型聚合搜索
- 📤 系统分享面板 + 批量导出
- 🔒 **完全本地**：无网络权限、无追踪、无数据上报

## 下载

[GitHub Releases](https://github.com/<user>/pixcase/releases) 下载最新 APK

最低 Android 版本：8.0 (API 26)

## 开发

```bash
git clone https://github.com/<user>/pixcase.git
cd pixcase
./gradlew assembleDebug
```

## 架构与文档

完整开发计划见 [`DEVELOPMENT_PLAN.md`](相册管理工具开发计划.md)

## 贡献

欢迎 PR！详见 [CONTRIBUTING.md](CONTRIBUTING.md)

## 协议

[Apache 2.0](LICENSE)
```

#### 1.2.3 CONTRIBUTING.md 要点

- 提 PR 前必须跑通 `./gradlew check`（ktlint + detekt + lint + test）
- Commit message 推荐 [Conventional Commits](https://www.conventionalcommits.org/)：`feat: ` / `fix: ` / `docs: ` / `refactor: `
- 单一 PR 不超过 400 行 diff（鼓励小步提交）
- 新增依赖需在 PR 描述中说明用途和 License 兼容性
- UI 改动必须附截图或录屏

#### 1.2.4 Issue / PR 模板

**bug_report.yml**：
```yaml
name: Bug Report
description: 报告 Bug
labels: ["bug"]
body:
  - type: textarea
    attributes: { label: 复现步骤, placeholder: "1. ...\n2. ..." }
  - type: input
    attributes: { label: 设备型号, placeholder: "Pixel 7 / Xiaomi 13 ..." }
  - type: input
    attributes: { label: Android 版本, placeholder: "Android 14" }
  - type: input
    attributes: { label: Pixcase 版本, placeholder: "v0.2.5" }
  - type: textarea
    attributes: { label: 日志, placeholder: "从 logcat 过滤 Pixcase/ 复制粘贴" }
```

**PR 模板**：
```markdown
## 变更类型
- [ ] Bug fix
- [ ] New feature
- [ ] Refactor / Docs

## 变更内容
<!-- 简述做了什么 -->

## 自检
- [ ] `./gradlew check` 全绿
- [ ] 已在自己主力机真机测试
- [ ] 新功能附截图 / 录屏
- [ ] CHANGELOG.md 已更新

关联 Issue: #
```

#### 1.2.5 SECURITY.md

```markdown
# 安全策略

## 支持版本

| 版本 | 支持状态 |
|---|---|
| 最新 release | ✅ 积极维护 |
| 前一个 release | ⚠️ 仅修复严重安全问题 |
| 更早版本 | ❌ 不再支持 |

## 报告漏洞

请**不要**在公开 Issue 报告安全漏洞。
邮件至 `<your-email>@example.com`，72 小时内回复。
```

#### 1.2.6 CHANGELOG.md 自动化

- 工具：[release-please](https://github.com/googleapis/release-please) GitHub Action
- 流程：合并 PR → Action 自动更新 CHANGELOG + bump version → 创建 Release PR → merge 后自动构建 APK + 上传 GitHub Release
- 本地手动：在 `CHANGELOG.md` 顶部加 `## [Unreleased]` 区段

#### 1.2.7 任务清单

- [ ] 仓库初始化：`LICENSE`（Apache 2.0）+ `NOTICE`
- [ ] 撰写 `README.md`（含截图占位、特性列表、下载链接）
- [ ] 撰写 `CONTRIBUTING.md` + `CODE_OF_CONDUCT.md`
- [ ] 创建 `.github/ISSUE_TEMPLATE/*.yml` 与 `PULL_REQUEST_TEMPLATE.md`
- [ ] 撰写 `SECURITY.md`
- [ ] 集成 `release-please` GitHub Action
- [ ] 第一次发版打 tag `v0.1.0` + 写 GitHub Release 说明

---

## 二、版本管理与发布架构（贯穿全程）

> App 一旦发布就要考虑演进。版本号、数据库迁移、更新检测、开源治理从阶段 0 就必须落到代码与 CI 上。

### 2.1 版本号体系（双轨制）

| 维度 | 字段 | 类型 | 用途 | 规范 |
|---|---|---|---|---|
| 内部版本号 | `versionCode` | `Integer` | 系统判断升级；强制单调递增 | 每次发版 +1；可用 `git rev-list HEAD --count` 自动生成 |
| 用户可见版本 | `versionName` | `String` | 商店与 App 内显示 | **语义化版本** `MAJOR.MINOR.PATCH`（如 `0.1.0` `1.2.3`） |
| 构建号 | `buildNumber` | `String` | CI/CD 追踪，可选 | `versionName-buildNumber-timestamp`（如 `1.0.0-001-20260819`） |

**版本阶段约定**（语义化版本前置规则）：

| 阶段 | versionName | 说明 |
|---|---|---|
| 内测 | `0.x.y` | 0.1 → 0.9 功能快速迭代 |
| 公测 | `0.x.0` | 0.5.0 / 0.9.0 等可对外送测 |
| 正式 | `1.0.0+` | 第一个稳定版 |
| 维护 | `x.y.z` | y/z 递增表示新增/修复 |

**配置位置**（`app/build.gradle.kts`）：

```kotlin
android {
    namespace = "com.example.pixcase"
    defaultConfig {
        applicationId = "com.example.pixcase"
        versionCode = 1
        versionName = "0.1.0"
    }
    buildTypes {
        release {
            isMinifyEnabled = true
        }
        debug {
            applicationIdSuffix = ".debug"
            versionNameSuffix = "-debug"
        }
    }
    flavorDimensions += "channel"
    productFlavors {
        create("huawei") { dimension = "channel" }
        create("google") { dimension = "channel" }
    }
}
```

### 2.2 AndroidManifest.xml 关键字段

```xml
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:label="@string/app_name"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:theme="@style/Theme.Pixcase"
        android:allowBackup="false"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:supportsRtl="true">

        <meta-data
            android:name="android.max_aspect"
            android:value="2.4" />

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:configChanges="orientation|screenSize|keyboardHidden">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
</manifest>
```

`@string/app_name` 等所有面向用户的字符串放 `res/values/strings.xml`，**支持多语言**（`values-zh-rCN` / `values-en` 等）。

### 2.3 数据迁移策略（Room Migration）

| 数据库版本 | 触发动作 | 风险 |
|---|---|---|
| 升级 | `MIGRATION_x_y` 自动执行 | 必须为每个版本写迁移；遗漏则崩溃 |
| 降级 | 卸载重装 | 通常不允许；提示用户卸载 |
| 强制升级 | 数据库结构重大变化 | 备份旧库 → 删除 → 重建 → 迁移数据 |

**Room Migration 示例**：

```kotlin
val MIGRATION_1_2 = object : Migration(1, 2) {
    override fun migrate(db: SupportSQLiteDatabase) {
        db.execSQL("ALTER TABLE tag ADD COLUMN type TEXT NOT NULL DEFAULT 'MARK'")
        db.execSQL("CREATE INDEX index_tag_name ON tag(name)")
    }
}

val MIGRATION_2_3 = object : Migration(2, 3) {
    override fun migrate(db: SupportSQLiteDatabase) {
        db.execSQL("""
            CREATE TABLE IF NOT EXISTS image_category (
                id INTEGER PRIMARY KEY AUTOINCREMENT,
                name TEXT NOT NULL,
                colorArgb INTEGER NOT NULL,
                coverMediaId INTEGER,
                usageCount INTEGER NOT NULL DEFAULT 0,
                createdAt INTEGER NOT NULL,
                updatedAt INTEGER NOT NULL
            )
        """.trimIndent())
        db.execSQL("CREATE UNIQUE INDEX index_image_category_name ON image_category(name)")
    }
}

val AppDatabase = Room.databaseBuilder(...)
    .addMigrations(MIGRATION_1_2, MIGRATION_2_3)
    .fallbackToDestructiveMigrationOnDowngrade()
    .build()
```

**架构原则**：

- **每个表的所有字段都必须有默认值**（新增 `NOT NULL` 字段必带 `DEFAULT`），保证迁移不丢数据
- **Schema 变更必须先写 Migration 再写代码**，CI 加 `room.schemaLocation` 自动生成 schema JSON 入库
- **永远不要 `fallbackToDestructiveMigration()`**——会清空用户全部元数据
- **媒体 ID 用 MediaStore `_ID` 是稳定的**：只要照片没被删/被系统清理，`_ID` 不变

### 2.4 更新检查机制（主推 GitHub Releases）

| 渠道 | 方式 | 适用 |
|---|---|---|
| **GitHub Releases** ⭐主推 | App 内 `GET https://api.github.com/repos/<user>/pixcase/releases/latest` | 开源项目标配；用户手动下载 APK |
| **Google Play** | 商店自动更新，用户无感知 | 若后续上架（可选） |
| **国内应用市场** | 各市场自带更新机制 | 若后续上架（可选） |
| **自托管 JSON** | 托管 `version.json`（`{ latest, url, force }`） | 私有部署 |

**GitHub Releases 检测实现示例**：

```kotlin
class UpdateCheckerImpl @Inject constructor(
    private val client: OkHttpClient,
    @ApplicationContext private val ctx: Context
) : UpdateChecker {
    override suspend fun check(): UpdateInfo? {
        val req = Request.Builder()
            .url("https://api.github.com/repos/<user>/<repo>/releases/latest")
            .build()
        return withContext(Dispatchers.IO) {
            client.newCall(req).execute().use { resp ->
                val json = Json.parseToJsonElement(resp.body!!.string()).jsonObject
                val tag = json["tag_name"]!!.jsonPrimitive.content.removePrefix("v")
                val apk = json["assets"]!!.jsonArray
                    .first { it.jsonObject["name"]!!.jsonPrimitive.content.endsWith(".apk") }
                    .jsonObject["browser_download_url"]!!.jsonPrimitive.content

                val current = ctx.packageManager.getPackageInfo(ctx.packageName, 0).versionName
                if (isNewer(tag, current)) UpdateInfo(tag, apk, force = false) else null
            }
        }
    }
}
```

### 2.5 提示性更新（开源项目不强制）

> **本项目为开源工具**，不强制用户升级——用户对设备拥有完全主权。版本演进通过兼容性设计 + 提示性通知完成。

| 场景 | 策略 |
|---|---|
| 数据库结构破坏性变更 | **不允许**；通过 Migration 维持向后兼容 |
| 关键功能下线 | **不直接下线**，保留但标记 deprecated |
| 安全漏洞修复 | **强烈提示**：通知栏 + 设置页红点 + About 页顶部 banner |
| 普通功能新增 | 提示性更新：通知栏 + 设置页红点 |
| Bug 修复 | 可选更新 |

**提示性更新实现**：

```kotlin
@Composable
fun AppRoot(updateInfo: UpdateInfo?) {
    // 不拦截启动，仅显示顶部 banner
    Column {
        updateInfo?.let { UpdateBanner(it) }  // 可关闭
        MainNavGraph()
    }
}
```

**数据迁移兜底**：由于不开强制更新，所有 Migration 必须**永远向前兼容**，哪怕用户跳过多个版本也能正常升级。`fallbackToDestructiveMigration()` 仍然禁用——版本跨度大时优先引导用户去 GitHub Issues 报告，保留其元数据。

### 2.6 后台下载与安装

- **下载**：`WorkManager` + `DownloadManager`（系统级，跨进程，断点续传）
- **安装**：API 26+ 通过 `FileProvider` + `ACTION_INSTALL_PACKAGE` Intent
- **进度通知**：`NotificationCompat` 显示下载进度（API 33+ 需 `POST_NOTIFICATIONS` 权限）
- **下载失败**：重试 3 次，最终提示用户去应用商店手动下载

### 2.7 应用商店上架元数据（**可选**，主分发渠道为 GitHub Releases）

> 本项目主分发渠道是 **GitHub Releases**（见 § 1.2 / § 2.6）。上架应用商店仅作为可选曝光途径，本节内容仅供未来上架时参考。

**Google Play Console 必填项**（若上架）：

| 项 | 说明 | 示例 |
|---|---|---|
| 应用名称 | ≤ 30 字符 | 图匣 Pixcase |
| 简短描述 | ≤ 80 字符 | 轻量、本地、强大的 Android 相册管理工具 |
| 完整描述 | ≤ 4000 字符 | 含功能列表、隐私声明、截图说明 |
| 应用图标 | 512×512 PNG，32-bit | `ic_launcher` |
| 特性图形 | 1024×500 PNG | 横版宣传图 |
| 截图 | 至少 2 张，最多 8 张 | 各分辨率各 1-2 张 |
| 类别 | 主类别 + 次类别 | **主：摄影 (PHOTOGRAPHY)**；次：工具 (TOOLS) |
| 内容分级 | 问卷自动生成 | 通常「所有人」 |
| 隐私政策链接 | 必填，公网可访问 | `https://<user>.github.io/pixcase/privacy`（用 GitHub Pages 托管） |
| 数据安全 | API 26+ 必填 | **本项目：不收集任何数据**（全勾"否"） |
| 目标受众 | 选择年龄段 | 通常 13+ 或 18+ |
| 开发者信息 | 姓名、邮箱、电话 | 必填 |
| 价格 | 免费 | `Free` |

**国内应用市场补充材料**（若上架）：与 Google Play 类似，额外需：软件著作权证书、ICP 备案（无网络可免）、实名认证；签名使用 V2/V3/V4。

### 2.8 App 内「关于」页面规范

```
┌─────────────────────────────────────┐
│              图匣 Pixcase             │
│           版本 0.1.0 (build 1)       │
│                                       │
│  © 2026 <你的名字>.                    │
│  Licensed under the Apache License,  │
│  Version 2.0.                         │
│                                       │
│  本应用为开源软件，欢迎 Star、Fork、    │
│  提 Issue 与 PR。                      │
│                                       │
│  ─────────────────────────────────    │
│                                       │
│  🌐 项目主页：github.com/<user>/pixcase│
│  🐛 报告问题：GitHub Issues           │
│  💬 讨论交流：GitHub Discussions      │
│  📜 隐私政策：<user>.github.io/...    │
│  📋 第三方开源许可                    │
│  🔄 检查更新                          │
│  ⭐ 给个 Star                        │
│                                       │
│  ─────────────────────────────────    │
│                                       │
│  设备信息（点击复制）：                │
│  Android 14 · API 34 · Xiaomi 13     │
│  App 内存：128 MB                     │
│  数据库版本：2                        │
│  Room Schema 版本：3                  │
└─────────────────────────────────────┘
```

| 项 | 实现 |
|---|---|
| 版本号读取 | `PackageManager.getPackageInfo(packageName, 0).versionName` + `longVersionCode` |
| Build 号 | 自定义 `BuildConfig.BUILD_NUMBER` |
| 设备信息 | `Build.MANUFACTURER` / `Build.MODEL` / `Build.VERSION.RELEASE` |
| 点击复制 | `ClipboardManager` + Toast |
| 检查更新 | 接入 2.4 的 `UpdateChecker`（GitHub Releases API） |
| 第三方许可 | 自动生成：用 `com.mikepenz:aboutlibraries` 插件从 `libs.versions.toml` 解析 |
| Room Schema 版本 | 从打包 assets 里的 `schemas/{db}.json` 文件名读取 |
| ⭐ Star 按钮 | `Intent.ACTION_VIEW` 跳 GitHub 仓库页 |

### 2.9 第三方依赖 License 管理

```kotlin
// app/build.gradle.kts
plugins {
    id("com.mikepenz.aboutlibraries.plugin") version "11.1.0"
}

aboutLibraries {
    libraryConfigFile = "config.gradle.kts"
}
```

效果：App 内一键展示所有依赖库的 License 列表，符合 Apache 2.0 / GPL 等开源协议合规要求。

### 2.10 国内上架合规要点（**可选**，仅上架时参考）

| 项 | 是否需要 | 说明 |
|---|---|---|
| 隐私政策弹窗 | ✅ | 首次启动弹窗，用户主动确认 |
| 权限申请说明 | ✅ | 系统级权限弹窗前，App 内先说明用途 |
| 收集个人信息清单 | ✅ | 本项目：**不收集任何个人信息** |
| 第三方 SDK 披露 | ✅ | 列出 ML Kit / Coil / 等 SDK 名称与用途 |
| 实名认证 | 视情况 | 个人开发者身份证（仅上架时需要） |
| ICP 备案 | ❌ | 无网络服务可免 |
| 等保测评 | ❌ | 非关键信息基础设施 |

**对本项目最重要的隐私承诺**：App 内不申请 `INTERNET` 权限（在 `AndroidManifest.xml` 中**显式移除**），从根源杜绝任何数据上传——这一项无论是否上架都建议做到。

### 2.11 任务清单

- [ ] `app/build.gradle.kts` 配置 `versionCode` / `versionName`，加 debug 后缀
- [ ] `AndroidManifest.xml` 设置 `application` 标签的 label / icon / theme / backup
- [ ] `res/values/strings.xml` 与多语言（至少中英）
- [ ] 编写隐私政策与用户协议
- [ ] `aboutlibraries` Gradle 插件集成
- [ ] `Settings → About` Composable：版本号、版权、链接、第三方许可入口
- [ ] 应用图标设计（前景 + 背景 + 自适应图标）
- [ ] 启动闪屏（Splash Screen API，Android 12+）
- [ ] 首次启动隐私政策弹窗（用户主动勾选）
- [ ] 准备上架素材（可选）：截图 6 张 + Feature Graphic + 应用图标
- [ ] 隐私承诺落实：`AndroidManifest.xml` **移除 `<uses-permission android:name="android.permission.INTERNET" />`**（若从未引入）
- [ ] CI/CD：GitHub Actions 自动构建 release APK + 上传 GitHub Release
- [ ] Room Migration 框架就位（每个新表/字段对应一个 `Migration_x_y`）
- [ ] `UpdateChecker` 实现（GitHub Releases API）
- [ ] 开源治理：完成 § 1.2 所有文件（`LICENSE` / `README.md` / `CONTRIBUTING.md` / Issue 模板 / `SECURITY.md` 等）
- [ ] 接入 `release-please` 实现 CHANGELOG 自动化

---

## 三、工程质量基线（贯穿全程）

> 性能、规范、不允许警告、CI/CD 是项目第一天就要立的标准，不是上线前才补的工程。

### 3.1 性能基线（按图库规模分级）

| 库规模 | 典型用户 | 关键指标 |
|---|---|---|
| 小批量 | < 1,000 张 | 首屏 < 300ms，滑动 60fps |
| 中批量 | 1,000 – 10,000 | 首屏 < 500ms，搜索 < 300ms |
| 大批量 | 10,000 – 50,000 | 首屏 < 800ms，Paging 加载无白屏 |
| 超大批量 | > 50,000 | 缩略图懒加载 + 预取 nextPage，搜索强制走索引 |

**核心性能指标（每个版本必须达标）**：

| 操作 | 指标 |
|---|---|
| 冷启动到时间线可见 | < 1.5s（中端机） |
| 时间线滚动 FPS | ≥ 55fps |
| 缩略图加载（首屏） | < 200ms/张（命中缓存时） |
| 全屏查看器切换 | < 100ms |
| 搜索响应 | < 300ms（10K 库） |
| 标记写入 | < 50ms |

### 3.2 关键性能优化点

| 优化点 | 实现 | 优先级 |
|---|---|---|
| MediaStore 查询只取必要列 | `projection` 只声明 `_ID, _DISPLAY_NAME, DATE_ADDED, BUCKET_DISPLAY_NAME, MIME_TYPE` | 高 |
| 缩略图走系统缓存 | `ContentResolver.loadThumbnail()`（API 29+） | 高 |
| Coil 自定义 Fetcher | 包装 `loadThumbnail`，复用系统缓存 | 高 |
| LazyVerticalGrid 优化 | `key = { it.id }` + `contentType = { it.mimeType }` | 高 |
| Room 索引 | 所有外键列 + 搜索字段都加 `indices` | 高 |
| 搜索先查 Room 再关联 MediaStore | 避免全表扫描 MediaStore | 高 |
| EXIF 懒读取 | 进入查看器详情才读 `ExifInterface` | 中 |
| 图片预解码 | Coil 默认开启 | 中 |
| 内存压力监听 | `ComponentCallbacks2.onTrimMemory` 主动清缓存 | 中 |
| Compose 重组优化 | `derivedStateOf` / `remember` 包裹重型计算 | 中 |
| 后台预加载下一页 | Paging 3 `prefetchDistance = 10` | 中 |
| 数据库分页 | Room `PagingSource<Int, T>` | 高 |
| Coil 磁盘缓存 | `imageLoader.diskCache` 配置 100MB | 中 |

### 3.3 过度设计黑名单（不要碰）

> 不要为了「看起来专业」而堆砌以下内容，会让代码膨胀、性能下降、维护成本飙升。

| 反模式 | 替代方案 |
|---|---|
| Domain 层 + UseCase（业务简单时） | ViewModel 直接调 Repository |
| 多 Module 拆分（功能少时） | 单 Module，按 package 分层 |
| 自定义注解 + KSP processor | 用 Room/Coil 等成熟库 |
| Hilt 多 Module 分层 | 一个 `AppModule` 够了 |
| DataStore 存所有设置（少量时） | SharedPreferences 即可 |
| WorkManager 做「预加载」 | Paging 3 自带预取 |
| 自研响应式框架 | Flow + StateFlow 已足够 |
| 微服务化 / 抽象网络层（本项目无网络） | 不需要 |
| 过度抽象 Repository 接口 | 直接 `class PhotoRepository @Inject constructor(...)` |
| 多套主题切换引擎 | 一个 `Theme` composable |
| 自研 DI 容器 | Hilt |

**判断标准**：「**当下只用一次 + 未来 6 个月内不会复用**」就**不抽象**。

### 3.4 不允许警告（警告视为错误）

> 类比 MSVC 的 `/WX`，Android 工具链原生支持。

**Kotlin 编译（`build.gradle.kts`）**：

```kotlin
tasks.withType<org.jetbrains.kotlin.gradle.tasks.KotlinCompile>().configureEach {
    kotlinOptions {
        allWarningsAsErrors = true
        freeCompilerArgs += listOf(
            "-Xexplicit-api=strict",
            "-Xjsr305=strict",
            "-opt-in=kotlin.RequiresOptIn",
            "-opt-in=androidx.compose.material3.ExperimentalMaterial3Api",
            "-opt-in=kotlinx.coroutines.ExperimentalCoroutinesApi"
        )
    }
}
```

**Android Lint**：

```kotlin
android {
    lint {
        abortOnError = true
        warningsAsErrors = true
        checkReleaseBuilds = true
        baseline = file("lint-baseline.xml")
        disable += setOf("MissingTranslation")
    }
}
```

**Java 编译**：

```kotlin
tasks.withType<JavaCompile>().configureEach {
    options.compilerArgs.add("-Xlint:all")
    options.compilerArgs.add("-Werror")
}
```

**依赖检查**：

```kotlin
configurations.all {
    resolutionStrategy.eachDependency {
        if (requested.version.contains("alpha") || requested.version.contains("beta")) {
            logger.warn("⚠️ 引入非稳定版依赖: ${requested.group}:${requested.name}:${requested.version}")
        }
    }
}
```

**编辑器与 IDE**：

| 工具 | 配置 |
|---|---|
| Android Studio | `Settings → Editor → Inspections` 全开，关键项标红 |
| ktlint | Gradle 插件，CI 跑 `gradlew ktlintCheck` |
| detekt | Gradle 插件，配置 `failFast = true` |

### 3.5 持续工程质量

| 项 | 工具 | 触发时机 |
|---|---|---|
| 单元测试 | JUnit4 + Turbine + MockK | ViewModel / Repository / DAO |
| UI 测试 | Compose `createComposeRule()` | 关键流程 |
| 截图测试 | Roborazzi / Paparazzi | 主题切换、组件稳定性 |
| 代码风格 | ktlint | pre-commit hook + CI |
| 静态分析 | detekt | CI |
| Lint | Android Lint | CI |
| 构建 | GitHub Actions | push / PR |
| 覆盖率 | Kover / Jacoco | CI，要求 ≥ 60% |

**CI 工作流（GitHub Actions）**：

```yaml
name: ci
on: [push, pull_request]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-java@v4
        with: { java-version: 17 }
      - run: ./gradlew ktlintCheck
      - run: ./gradlew detekt
      - run: ./gradlew lint
      - run: ./gradlew test
      - run: ./gradlew assembleDebug
```

---

## 四、目录结构

```
app/src/main/java/com/example/pixcase/
├── PixcaseApp.kt                  # @HiltAndroidApp
├── MainActivity.kt                # 单 Activity 入口
├── core/
│   ├── util/                      # 扩展函数、时间格式化等
│   ├── permission/                # 权限封装
│   └── result/                    # Resource / Result 包装
├── data/
│   ├── mediastore/                # MediaStore 数据源 + Cursor 映射
│   ├── db/
│   │   ├── AppDatabase.kt
│   │   ├── entity/                # 所有 Entity（相册/标签/收藏/隐藏/印象/分类/会话/决策）
│   │   └── dao/
│   ├── repository/                # PhotoRepository / AlbumRepository / TagRepository / MarkRepository / CategoryRepository / QuickSortRepository
│   └── model/                     # MediaPhoto / Album / Tag（领域模型）
├── domain/
│   └── usecase/                   # 用例（可选，规模不大时可省略）
├── ui/
│   ├── theme/                     # Material3 主题
│   ├── common/                    # 通用 Composable (EmptyState, Loading ...)
│   ├── feature/
│   │   ├── timeline/              # 时间线主界面（含顶部搜索框）
│   │   ├── viewer/                # 全屏查看器（含印象标记入口）
│   │   ├── albums/                # 相册列表 + 详情
│   │   ├── tags/                  # 标签管理
│   │   ├── qtsort/                # 快速整理（Tinder 式卡片）
│   │   ├── search/                # 工具二：搜索
│   │   │   ├── result/            # 搜索结果页（多选 + 分享）
│   │   │   └── marks/             # 单图印象标记编辑
│   │   ├── category/              # 工具二：大类分类管理
│   │   ├── favorites/             # 收藏
│   │   ├── hidden/                # 隐藏
│   │   └── settings/              # 设置（含关于、隐私、第三方许可）
│   └── navigation/                # NavGraph
└── di/                            # Hilt Module
```

---

## 五、数据库设计

```kotlin
// 用户自定义相册
@Entity(tableName = "album")
data class AlbumEntity(
    @PrimaryKey(autoGenerate = true) val id: Long = 0,
    val name: String,
    val coverMediaId: Long?,
    val createdAt: Long,
    val updatedAt: Long,
    val isSystem: Boolean = false,
    val systemType: String? = null
)

// 相册-照片多对多
@Entity(
    tableName = "album_photo",
    primaryKeys = ["albumId", "mediaId"],
    indices = [Index("mediaId")]
)
data class AlbumPhotoCrossRef(
    val albumId: Long,
    val mediaId: Long,
    val addedAt: Long
)

// 标签（自定义相册用）
@Entity(tableName = "tag")
data class TagEntity(
    @PrimaryKey(autoGenerate = true) val id: Long = 0,
    val name: String,
    val colorArgb: Int,
    val createdAt: Long
)

// 标签-照片多对多
@Entity(
    tableName = "photo_tag",
    primaryKeys = ["tagId", "mediaId"],
    indices = [Index("mediaId")]
)
data class PhotoTagCrossRef(val tagId: Long, val mediaId: Long)

// 收藏
@Entity(tableName = "favorite")
data class FavoriteEntity(@PrimaryKey val mediaId: Long, val addedAt: Long)

// 隐藏
@Entity(tableName = "hidden_photo")
data class HiddenEntity(@PrimaryKey val mediaId: Long, val addedAt: Long)

// 搜索历史
@Entity(tableName = "search_history")
data class SearchHistoryEntity(
    @PrimaryKey(autoGenerate = true) val id: Long = 0,
    val query: String,
    val timestamp: Long
)

// 快速整理会话
@Entity(tableName = "quick_sort_session")
data class QuickSortSessionEntity(
    @PrimaryKey val albumId: Long,
    val startedAt: Long,
    val lastActiveAt: Long,
    val targetAlbumIds: String,
    val totalCount: Int,
    val processedCount: Int,
    val currentIndex: Int,
    val isCompleted: Boolean = false
)

// 快速整理每张照片的决策
@Entity(
    tableName = "quick_sort_decision",
    primaryKeys = ["sessionAlbumId", "mediaId"],
    indices = [Index("mediaId")]
)
data class QuickSortDecisionEntity(
    val sessionAlbumId: Long,
    val mediaId: Long,
    val decision: String,
    val decidedAt: Long
)

// === 工具二：图像标记与快速搜索（独立 schema） ===

// 大类分类
@Entity(
    tableName = "image_category",
    indices = [Index(value = ["name"], unique = true)]
)
data class ImageCategoryEntity(
    @PrimaryKey(autoGenerate = true) val id: Long = 0,
    val name: String,
    val colorArgb: Int,
    val coverMediaId: Long? = null,
    val usageCount: Int = 0,
    val createdAt: Long,
    val updatedAt: Long
)

// 分类-照片多对多
@Entity(
    tableName = "image_category_cross",
    primaryKeys = ["categoryId", "mediaId"],
    indices = [Index("mediaId")]
)
data class ImageCategoryCrossRef(
    val categoryId: Long,
    val mediaId: Long,
    val addedAt: Long
)

// 印象标记
@Entity(
    tableName = "image_mark",
    primaryKeys = ["mediaId", "markNameNormalized"],
    indices = [Index("markNameNormalized")]
)
data class ImageMarkEntity(
    val mediaId: Long,
    val markNameOriginal: String,
    val markNameNormalized: String,
    val createdAt: Long
)
```

**对外只读模型**（UI 层使用）：

```kotlin
data class MediaPhoto(
    val id: Long,
    val uri: Uri,
    val displayName: String,
    val mimeType: String,
    val dateAddedSec: Long,
    val dateTakenMs: Long?,
    val width: Int,
    val height: Int,
    val sizeBytes: Long,
    val durationMs: Long?,
    val bucketName: String?,
    val relativePath: String?,
    val lat: Double?, lon: Double?,
    val isFavorite: Boolean,
    val isHidden: Boolean,
    val tags: List<Tag>,
    val categories: List<ImageCategory>,
    val marks: List<String>
)
```

---

## 六、分阶段开发计划

### 阶段 0 — 工程初始化（预计 1–2 天）

**任务**
- [ ] 用 Android Studio 创建 `Empty Compose Activity` 项目，包名 `com.example.pixcase`
- [ ] 配置 `libs.versions.toml`（Version Catalog），统一管理依赖
- [ ] 启用 Kotlin 2.0、KSP、Hilt、Compose Compiler Gradle Plugin
- [ ] 配置 `build.gradle.kts`：minSdk=26、targetSdk=35、Java 17、`versionCode` / `versionName`、`lint { abortOnError = true }`、`allWarningsAsErrors`
- [ ] 添加权限到 `AndroidManifest.xml`
- [ ] 实现 `PermissionHelper`
- [ ] 主题：Material 3 + Dynamic Color + 深色模式
- [ ] Hilt：`PixcaseApp` 加 `@HiltAndroidApp`，`MainActivity` 加 `@AndroidEntryPoint`
- [ ] Navigation：搭建空骨架（5 个顶级路由）
- [ ] CI：GitHub Actions 跑 `ktlintCheck` / `detekt` / `lint` / `test`

**产出**：可编译运行、空壳 App；CI 红绿可见；任何 PR 触发自动校验。

---

### 阶段 1 — 基础相册浏览（MVP，3–5 天）

**任务**
- [ ] `MediaStoreDataSource`：封装 `queryImages()`、`queryVideos()`，返回 `Flow<PagingData<MediaPhoto>>`
- [ ] `PhotoRepository`：合并 MediaStore + Room
- [ ] 时间线主界面 `TimelineScreen`（按 `dateTakenMs` 分组，3 列网格，Sticky header）
- [ ] 全屏查看器 `ViewerScreen`（`HorizontalPager` + `Modifier.transformable`）
- [ ] 缩略图优化：`MediaStore.loadThumbnail()` + Coil 自定义 Fetcher
- [ ] 下拉刷新、空状态、加载骨架屏

**验收**：打开 App → 时间线 → 全屏查看流畅；中端机 10K 库冷启动 < 1.5s。

---

### 阶段 2 — 图像操作（2–3 天）

**任务**
- [ ] 多选模式：长按进入 `SelectionMode`
- [ ] 操作项：删除、收藏、移动、添加标签、分享、设为壁纸、隐藏
- [ ] **删除**：调用 `ContentResolver.delete()`，原图进入系统相册回收站
- [ ] **收藏**：写入 Room `favorite` + 同步 `MediaStore.IS_FAVORITE`
- [ ] **隐藏**：写入 Room `hidden_photo`，查询时过滤
- [ ] **重命名**：`MediaStore.updateDisplayName()`
- [ ] **移动文件夹**：SAF + `RELATIVE_PATH`
- [ ] **旋转**：写回 EXIF 方向
- [ ] **设为壁纸**：`WallpaperManager.setBitmap()`

---

### 阶段 3 — 分类与标签管理（3–4 天）

**任务**
- [ ] Room 数据访问层 + DAO Flow
- [ ] **自定义相册**：创建 / 重命名 / 删除 / 详情页
- [ ] **智能相册**：截图 / 自拍 / 视频 / 收藏
- [ ] **标签系统**：创建标签、给照片打标签、多标签 AND/OR 筛选
- [ ] 相册封面：自动选最近一张或用户手动指定

---

### 阶段 3.5 — 快速整理模式（Tinder 式卡片，4–6 天）

> 类似 Tinder / 探探 的卡片堆叠 + 拖拽交互，把大相册照片高速分流到其他相册。

#### 3.5.1 功能定义

| 项 | 说明 |
|---|---|
| 入口 | 自定义相册详情页右上角 ⚡「快速整理」按钮 |
| 模式选择 | 首次启用 → 配置对话框：选择目标相册、选择「继续上次」/「从头开始」 |
| 主界面 | 类似 Tinder 的卡片堆叠，下方是目标相册 Dock |
| 卡片堆叠 | 最上层 = 当前照片，下层往右依次预览后续 2 张 |
| 退出 | 顶栏 ← 返回（自动保存进度，可下次继续） |

#### 3.5.2 手势与操作

| 手势 | 行为 | 视觉反馈 |
|---|---|---|
| **左划** | 保留 → 留在当前相册，标记已处理，下次不再弹出 | 卡片左飞、**左侧「保留」提示浮现** |
| **上划** | 移出当前相册；若照片只属于本相册，触发 MediaStore 真删除（原图进入系统相册回收站） | 卡片上飞、**顶部「🗑️ 删除」提示浮现** |
| **拖到 Dock** | 移动 → 从当前相册移出，加入目标相册 | Dock 高亮目标图标，卡片吸附 |
| **下划 / 右划** | 底部 Sheet 兜底选择 | 半透蒙层 |

**顶栏 ⟲ 重启按钮**：必须弹二次确认对话框（防误触），文案：
- 标题：「重新开始本次整理？」
- 内容：「已处理的 X 张照片将重新进入整理队列。目标相册设置会保留。」
- 主按钮：「确认重新开始」（destructive 红色）/ 次按钮：「取消」

#### 3.5.3 Session 与决策持久化

```
打开相册 A → 点 ⚡快速整理
   ├─ 检查 A 是否有未完成 session
   │     ├─ 有 → 弹 Sheet 让用户选择 [继续上次] / [从头开始]
   │     │     ├─ 继续：从 currentIndex 起加载 unprocessed 照片
   │     │     └─ 从头：删除旧 session 全部 decisions，重置计数
   │     └─ 无 → 直接进入配置流程
   └─ 配置：勾选目标相册 → 创建 session → 进入卡片界面
```

**业务规则**：

- `SKIP` 也算"已处理"——下次启动时不再显示，避免重复打扰
- "从头开始"只清空 decision 记录，不删原相册照片
- "继续上次"读取 session 的 `currentIndex` 与所有 decisions，过滤掉已决策的 media_id
- session 完成时（`processedCount ≥ totalCount`）置 `isCompleted = true`
- 媒体操作实际执行时机：决策发生时立即写库（事务：写 decision + 改 album_photo 关联 + 写 trash 或目标相册关联）

#### 3.5.4 删除语义：与系统相册回收站对齐

> **核心原则**：模仿 Android 系统相册的行为——上划删除的照片进入系统相册自带的「最近删除 / 回收站」，由系统在 30 天后自动清理。本 App **不维护单独的回收相册**。

**上划的双重语义**（根据照片所在相册数量自动分支）：

```
上划触发
  ↓
查询 SELECT COUNT(*) FROM album_photo WHERE mediaId = ?
  ├─ count == 1（仅属于当前相册）
  │     └─ 走 MediaStore 真删除 → 原图进入系统相册「回收站」
  │
  └─ count > 1（同时属于其他相册）
        └─ 仅删除 album_photo 当前关联，原图保留在系统相册
```

| 项 | 设计 |
|---|---|
| 真删除 API | API 30+ 优先 `MediaStore.createDeleteRequest()` 触发系统级确认；快速整理场景为避免打断手势流调用 `ContentResolver.delete()` 直接删除 |
| 事务保证 | 真删除 + `album_photo` 关联清理放在一个 `withTransaction` 里；MediaStore 删除抛异常时关联不删 |
| 撤销语义 | SnackBar 撤销按钮只能撤销「仅解绑」分支；真删除分支 SnackBar 提示「请到系统相册最近删除中恢复」 |
| 系统相册同步 | 真删除后监听 `ContentObserver` 触发 MediaStore 数据刷新 |
| 兜底兼容 | API ≤29 没有 createDeleteRequest，直接 `ContentResolver.delete()`；小米/华为/Vivo 厂商拦截 → SnackBar 提示用户去系统相册手动删除 |

#### 3.5.5 UI 结构与动画

```
┌─────────────────────────────────────────────┐
│  ← 相册A · 快速整理         23 / 187  ⟲     │  ← 顶栏：进度 + 重启（带二次确认）
├─────────────────────────────────────────────┤
│           ┌──────────┐                      │  ← 顶部「删除」提示
│           │  🗑️ 删除  │     （默认隐藏，     │     拖动时按位移/透明度淡入
│           └──────────┘        上滑手势触发）  │
│                                             │
│        ┌───────────────────┐                │
│        │   后面的照片预览   │ ← scale 0.90   │
│        └───────────────────┘                │
│      ┌───────────────────┐                  │
│      │    当前照片卡片    │ ← scale 1.00    │
│      └───────────────────┘                  │
│                                             │
│  ┌────────┐                                 │
│  │ ← 保留 │   （默认隐藏，左滑手势触发）    │  ← 左侧「保留」提示
│  └────────┘                                 │
├─────────────────────────────────────────────┤
│  📁相册B   📁相册C   📁相册D   ➕            │  ← Dock：只放目标相册
└─────────────────────────────────────────────┘
```

**四个方向反馈区域划分**：

| 区域 | 位置 | 触发条件 | 反馈强度 |
|---|---|---|---|
| 🗑️ 删除 | 屏幕顶部居中 | dy < -阈值 | 透明度 + scale 与 \|dy\| 成正比 |
| ← 保留 | 屏幕左侧中部 | dx < -阈值 | 透明度 + scale 与 \|dx\| 成正比 |
| 📁 目标相册 | 底部 Dock | 拖入图标 bounds | Dock 高亮发光 |
| 兜底 Sheet | 底部 | 下划 / 右划 / 长按 | 半透蒙层 + 操作列表 |

**Dock 末尾 ➕ → 目标相册管理界面**：

```
┌─────────────────────────────────────────────┐
│  ← 管理目标相册                  [保存]      │
├─────────────────────────────────────────────┤
│  已选 3 / 12                                 │
├─────────────────────────────────────────────┤
│  ☑️  📁 相册B                       ✓       │
│  ☑️  📁 相册C                       ✓       │
│  ☐   📁 相册D                              │
│  ☐   📁 相册E                              │
│  ☐   📁 相册F                              │
│  ...                                        │
│  ☐   📁 相册M                              │
├─────────────────────────────────────────────┤
│           ➕  创建新相册并加入                │
└─────────────────────────────────────────────┘
```

| 项 | 行为 |
|---|---|
| 列表项 | 全量自定义相册（**过滤掉系统相册**：截图/自拍/视频/收藏/最近删除）；每项 = 缩略图 + 名称 + 勾选状态 |
| 勾选 / 取消 | 直接点列表项切换 |
| ➕ 创建新相册 | 底部固定按钮 → 弹输入对话框（名称校验：非空、不与现有相册重名）→ `AlbumRepository.createAlbum()` → 自动勾选进入列表 |
| 保存 | 写入 `session.targetAlbumIds`，回到快速整理页，Dock 自动重渲染 |
| 取消 / 返回 | 丢弃本次选择变更，原 session 不变 |
| 边界 | 允许 0 个目标相册（保存时弹 SnackBar 提示「仅保留 / 删除可用」）；删除相册的入口不放此处 |

**动画规格**：

| 元素 | 实现 |
|---|---|
| 卡片堆叠层次 | 3 层可见，scale / offsetY / alpha 按等差递减（1.0→0.95→0.90） |
| 拖动跟随 | `pointerInput { detectDragGestures }`，offset 与 rotation 同步（rotation = offsetX / 20） |
| 飞出 | 触发阈值后 `Animatable.animateTo`，spring stiffness=400, dampingRatio=0.6 |
| 新卡片上浮 | 下一张卡片 `animateFloatAsState` 缩放从 0.95→1.0，时长 250ms |
| 拖到 Dock | 检测 overlap，target dock 高亮发光，释放后卡片"飞向"Dock 并淡出 |
| 顶部/侧边提示 | 与拖动 offset 联动：`alpha = (|offset| / threshold).coerceIn(0f, 1f)` |
| 性能 | 使用 `graphicsLayer` + `key(mediaId)`，避免重组；图片用 Coil 内存缓存 |

#### 3.5.6 任务清单

- [ ] Room Schema：`QuickSortSessionEntity` / `QuickSortDecisionEntity`，对应 DAO + Migration
- [ ] `QuickSortRepository`：session CRUD、决策记录、未处理照片查询
- [ ] 上划双重语义：根据 `album_photo` 中照片所属相册数决定是 MediaStore 真删除还是仅解绑
- [ ] `QuickSortSetupDialog`：选择目标相册、继续/重启选项
- [ ] `QuickSortScreen`：主界面框架
- [ ] `PhotoCardStack` Composable：卡片堆叠渲染
- [ ] `PhotoCard`：拖动手势、旋转反馈、飞出动画
- [ ] `AlbumDock`：目标相册图标列表（不含回收/最近删除）；末尾 ➕ 入口
- [ ] `QuickSortTargetPickerScreen`：目标相册多选界面 + 创建新相册
- [ ] 顶部「删除」提示层 + 左侧「保留」提示层
- [ ] 顶栏 ⟲ 重启二次确认 `AlertDialog`
- [ ] `QuickSortViewModel`：`StateFlow<QuickSortUiState>`
- [ ] 撤销机制（可选）：SnackBar 撤销最近一次决策，5 秒内可撤回
- [ ] 单元测试：session 续接逻辑、提示层显隐阈值、双重语义分支

**验收**：500 张照片相册里完成快速整理，正确分流到 3 个目标相册 + 删除 50 张（仅当前相册时进系统回收站），过程流畅无卡顿，下次进入跳过已处理项，「从头开始」可重置进度。

---

### 阶段 4 — 搜索功能（2–3 天）

**任务**
- [ ] 搜索栏（顶部，Material 3 `SearchBar`）
- [ ] 搜索维度：文件名 LIKE、自定义相册名/标签名（Room LIKE）、EXIF 相机型号
- [ ] 筛选面板（`ModalBottomSheet`）：时间范围、文件大小、尺寸、MIME 类型、收藏/隐藏/有标签
- [ ] 搜索历史（Room 存最近 20 条）
- [ ] 搜索建议（基于已有标签/相册名）
- [ ] 关键字高亮

**验收**：1000 张照片库下搜索响应 < 300ms。

---

### 阶段 5 — 分享与导出（1–2 天）

**任务**
- [ ] 单张分享：`ACTION_SEND`
- [ ] 多张分享：`ACTION_SEND_MULTIPLE`
- [ ] 分享前可选：压缩 / 清除 EXIF / 加水印
- [ ] 批量导出为 ZIP
- [ ] 复制到剪贴板
- [ ] 「分享到本应用」接收

---

### 阶段 7 — 图像标记与快速搜索工具（工具二，5–7 天）

> 工具定位：解决大量图像/表情包场景下「知道有却找不到」的痛点。通过给图像打**印象标记**（细粒度字符串）和**大类分类**（粗粒度预设分类），让搜索直达，并一键多选分享。

#### 7.1 与阶段 3 tag 的关系

| 项 | 决策 |
|---|---|
| 是否复用阶段 3 的 `tag` 表 | **不复用**，新建独立 schema（`image_category` / `image_mark`） |
| 理由 | tag 用于自定义相册精细标签管理；category/mark 是工具二的专用语义，混用会让「创建」「删除」「搜索」UX 都变复杂 |
| 阶段 3 tag 表 | **保留不动**，作为相册标签继续存在 |
| 互相查找 | 工具二搜索时**只查** image_category / image_mark，不查 tag |

#### 7.2 核心数据模型

- `ImageCategoryEntity`：大类分类，`name` 唯一，`usageCount` 冗余，封面图取最近一张
- `ImageCategoryCrossRef`：分类-照片多对多
- `ImageMarkEntity`：印象标记，**`primaryKeys = ["mediaId", "markNameNormalized"]`**——同一张照片同一字符串去重，但跨照片可重用同一字符串

#### 7.3 功能拆解

##### 7.3.1 主界面搜索框（常驻）

- `TimelineScreen` 顶部加 `SearchBar`（折叠态 = 输入框；展开态 = 全屏搜索）
- 输入即搜（debounce 200ms），聚合三类结果：
  1. **印象命中**：`image_mark.markNameNormalized LIKE '%query%'`
  2. **大类命中**：`image_category.name LIKE '%query%'`
  3. **文件名命中**：MediaStore `DISPLAY_NAME LIKE '%query%'`
- 三类结果用 Chip 分组
- 搜索结果列表 = 网格布局 + 多选模式
- 选中后 → 顶部操作栏「分享 N 张」按钮 → 调系统分享面板

##### 7.3.2 单图印象标记（自由输入）

- 入口：全屏查看器内长按 → 「编辑印象」；或查看器右下角悬浮「标签」按钮
- 弹出 `ImageMarksPanel`（`ModalBottomSheet`）：
  - 顶部「+ 添加印象」输入框 → 回车提交
  - 输入框带**已用印象自动补全**（基于 `image_mark` 已有的 `markNameOriginal`，前 10 条高频）
  - 下方列表 = 当前照片已打的印象（chip 形态，每个 ✕ 可移除）
- 持久化：归一化 `markNameNormalized = markNameOriginal.trim().lowercase()`

##### 7.3.3 大类分类（预设 + 拖拽分配）

- **分类管理界面** `CategoryManagerScreen`：
  - 顶部 ➕ 创建新分类（输入名称 + 选 8 色）→ 名称去重校验
  - 列表项：分类缩略图（封面图或默认图标）+ 名称 + 使用次数 + ⋮ 重命名/删除
  - 删除分类弹二次确认（destructive）
  - 点击分类项 → 进入该分类下的所有照片
- **拖拽分配** `DragToCategoryOverlay`：
  - 主时间线网格长按图片 0.5s → 进入拖拽态
  - 底部弹出**分类 Dock**——列出所有 `image_category`
  - 拖到某个分类上 → Dock 高亮 + 缩放
  - 释放 → 写入 `image_category_cross`，toast「已加入 XXX」
  - 多选拖拽：在阶段 2 的多选模式下点「加入分类」按钮 → 弹分类选择 Sheet

##### 7.3.4 搜索 UX 细节

| 维度 | 设计 |
|---|---|
| 搜索框位置 | 主界面顶部，常驻折叠态；点击展开全屏 |
| 输入响应 | debounce 200ms；空字符串清除结果显示全部时间线 |
| 结果排序 | 按 `dateTakenMs` DESC |
| 关键字高亮 | 搜索结果文件名/印象/分类名中命中的片段高亮 |
| 搜索历史 | 复用 `search_history` 表 |
| 搜索建议 | 聚合 `image_mark.markNameOriginal` + `image_category.name` 前 10 高频 |
| 空结果 | EmptyState 提示「没找到匹配的图像」 |
| 新手引导 | 首次打开搜索空结果时引导用户去打印象 |

#### 7.4 任务清单

- [ ] Room Schema + Migration：`ImageCategoryEntity` / `ImageCategoryCrossRef` / `ImageMarkEntity`
- [ ] `MarkRepository` / `CategoryRepository`
- [ ] `SearchUseCase`：聚合三类命中去重排序
- [ ] 改造 `TimelineScreen` 顶部加 `SearchBar`
- [ ] `SearchResultScreen`：结果网格 + 类型 Chip + 多选 + 分享
- [ ] `ImageMarksPanel`：印象编辑
- [ ] `CategoryManagerScreen`：分类列表 / 创建 / 重命名 / 删除
- [ ] `CategoryDetailScreen`：分类下图片列表
- [ ] `DragToCategoryOverlay`：主网格长按拖拽分类 dock
- [ ] 搜索历史 / 建议 / 高亮 / EmptyState / 新手引导
- [ ] 多选模式新增「加印象」「加分类」按钮
- [ ] 单元测试：归一化逻辑、PK 去重、搜索聚合性能
- [ ] 设备实测：5000 张打了印象的图片下搜索响应 < 150ms

**验收**：主界面顶部输入"点赞" → 0.2s 内出结果 → 多选 5 张 → 一键分享到微信成功。

---

### 阶段 8 — 高级特性（按优先级挑选，建议 2–4 周拆分）

| 优先级 | 功能 | 关键点 |
|---|---|---|
| ⭐⭐⭐ | **重复 / 相似照片检测** | pHash + 感知哈希，Bucketing；周报提示清理 |
| ⭐⭐⭐ | **人物相册**（本地 ML Kit Face Detection） | 检测人脸 → dHash / 平均人脸向量聚类 → 自动归组 |
| ⭐⭐ | **场景标签**（ML Kit Image Labeling） | 离线模型，识别「日落/海滩/食物」等自动打标签 |
| ⭐⭐ | **地图视图** | 用 `osmdroid`（免 API Key）或 Google Maps；按 GPS 聚合 |
| ⭐ | **AI 自动归档建议** | 基于场景标签 + 整理历史，对新照片提示建议归到哪个相册，用户一键确认 |
| ⭐ | **私密空间** | PIN 码 / 生物识别 + 加密元数据 |
| ⭐ | **Live Photo / 动态照片回放** | 解析 Motion Photo（API 30+ 系统支持） |
| ⭐ | **HEIF 转 JPEG 批量导出** | 旧设备分享前转换 |
| ⭐ | **自动备份** | WorkManager 周期任务，复制到用户选定目录 |

---

## 七、关键设计要点

### 1. MediaStore 性能
- 大库（>5 万张）必须分页：用 **Paging 3 + `ContentResolver.query`**，**不要一次 query 所有列**，只取缩略图需要的字段（`_ID, _DISPLAY_NAME, DATE_ADDED, BUCKET_DISPLAY_NAME, MIME_TYPE`），详情进查看器再补 `ORIENTATION / LATITUDE / LONGITUDE`。
- 监听变化：`ContentObserver` 监听 `MediaStore.Images.Media.EXTERNAL_CONTENT_URI`，触发 Paging 失效。

### 2. 权限策略
- API 33+：`READ_MEDIA_IMAGES / READ_MEDIA_VIDEO` 分两个权限请求，先图片够用。
- 首次启动分阶段：先要 `READ_MEDIA_IMAGES` → 能浏览 → 用户用「移动/创建相册」等高级功能时再要 `MANAGE_EXTERNAL_STORAGE` 或引导到 SAF。
- 权限被拒：引导去设置（`ACTION_APPLICATION_DETAILS_SETTINGS`），给出明确原因。

### 3. 缩略图策略
- 优先用 `ContentResolver.loadThumbnail()`（API 29+，系统级缓存，O(1)）。
- Coil 自定义 `Fetcher` 走 `loadThumbnail` + 内存/磁盘缓存。
- 长列表用 `LazyVerticalGrid` + `key` + `contentType`，避免重组。

### 4. 状态管理
- ViewModel 用 `StateFlow<UiState>`，UiState 是 `sealed interface`（Loading / Success / Empty / Error）。
- 跨页面状态（当前选中相册、筛选条件）放 Navigation 共享 ViewModel（`hiltViewModel(navController.getBackStackEntry(...))`）。

### 5. 测试与质量
- 单元测试：Repository、DAO、ViewModel（`JUnit4 + Turbine + MockK`）
- UI 测试：Compose `createComposeRule()` 覆盖关键流程（时间线 → 查看器 → 收藏）
- 设备矩阵：小米/华为/三星/原生 Pixel 各一台，覆盖厂商定制相册差异
- Lint：`detekt` + `ktlint` + Android Lint（**警告视为错误**，见 3.4）
- ProGuard：Compose、Room、Hilt、ML Kit 规则齐全，发布前跑 R8 全量

### 6. 厂商适配
- 小米/华为/Vivo 的「媒体存储」可能限制 MediaStore 写入 → 删除 / 移动走 SAF `createDeleteRequest` 触发系统确认，**避免**直接 `ContentResolver.delete` 被厂商拦截。
- 华为/荣耀的照片库时间字段异常 → 兜底用 `DATE_TAKEN` + `DATE_ADDED` + 文件 mtime 综合判断。

---

## 八、迭代节奏建议

| 阶段 | 周次 | 里程碑（可演示版本） |
|---|---|---|
| 0 + 1 | 第 1–2 周 | v0.1 — 基础浏览查看 |
| 2 + 3 | 第 3–4 周 | v0.2 — 操作 + 相册/标签管理 |
| **3.5** | **第 5 周** | **v0.2.5 — 快速整理模式上线** |
| 4 | 第 6 周 | v0.3 — 搜索（文件名/相册/标签） |
| 5 | 第 7 周 | v0.3.1 — 分享与导出 |
| **7** | **第 8–9 周** | **v0.4 — 工具二：标记搜索 + 分类拖拽** |
| 8 | 第 10–13 周 | v1.0 — 高级特性（按优先级挑选） |

> 建议每个阶段结束都打一个 **debug APK 装到自己的主力机** 实测一周，再进下一阶段。相册 App 的体感（手势流畅度、缩略图加载）只有真机跑得出来。

---

## 九、立刻可以开始的 5 件事

> 打开 Android Studio 前先确认这些前置规范已就位（详见第二、三章）。

1. **GitHub 仓库与开源治理**：在 GitHub 新建 `pixcase` 仓库（公开），按 § 1.2 一次写齐 `LICENSE`（Apache 2.0）/ `NOTICE` / `README.md` / `CONTRIBUTING.md` / `CODE_OF_CONDUCT.md` / `SECURITY.md` / Issue & PR 模板。**先把仓库立起来再写代码**，让第一次 commit 就合规。
2. **规范化项目骨架**：本地 clone 后用 Android Studio 打开，按 § 1.1 用 `Pixcase / 图匣 / com.example.pixcase` 命名；按 § 2.1 配置 `versionCode` / `versionName`；按 § 3.4 设置 `allWarningsAsErrors = true` + `lint { abortOnError = true }`；按 § 2.10 **移除 `INTERNET` 权限**。
3. **依赖骨架**：建立 `libs.versions.toml`，引入 Compose BOM、Room、Coil、Hilt、Navigation、Paging、WorkManager、ML Kit；集成 `aboutlibraries`、`ktlint`、`detekt` 三个 Gradle 插件（§ 2.9 / § 3.4）。
4. **CI 跑通**：建立 `.github/workflows/ci.yml`，跑 `ktlintCheck` / `detekt` / `lint` / `test` / `assembleDebug`，5 个 job 全绿即可（§ 3.5）。
5. **Room 框架与 Migration 占位**：建 `AppDatabase` 抽象类与当前所有 Entity，Migration 函数保留为 `TODO`；`MediaPhoto` 模型上加 `isFavorite / isHidden / tags / categories / marks` 占位字段，Repository 层返回 `Flow<List<MediaPhoto>>` 即可，UI 暂不消费。