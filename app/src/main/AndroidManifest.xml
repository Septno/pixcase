<?xml version="1.0" encoding="utf-8"?>
<!--
  本应用为完全本地的相册管理工具,**不申请 INTERNET 权限**(计划 § 2.10 隐私承诺)。
  所有用户照片仅在本机处理,元数据写入 Room,删除/移动通过 MediaStore API。

  WorkManager 配置走 PixcaseApp 的 Configuration.Provider(由 Hilt 注入),
  走 on-demand 初始化,所以**不需要** androidx.startup.InitializationProvider。
-->
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- 媒体读取权限(API 33+ 分两个,旧版本用 READ_EXTERNAL_STORAGE) -->
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <uses-permission android:name="android.permission.READ_MEDIA_VIDEO" />
    <uses-permission
        android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />
    <!-- API 33+ 通知(用于下载进度提示) -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- 选填:读取 EXIF GPS 信息 -->
    <uses-permission android:name="android.permission.ACCESS_MEDIA_LOCATION" />

    <!--
      ML Kit Face Detection / Image Labeling 硬件加速可选声明。
      不需要 manifest 额外权限,因为完全本地推理。
    -->

    <application
        android:name=".PixcaseApp"
        android:label="@string/app_name"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:theme="@style/Theme.Pixcase.Starting"
        android:allowBackup="false"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:supportsRtl="true"
        android:enableOnBackInvokedCallback="true"
        tools:targetApi="34">

        <!--
          WorkManager on-demand 初始化:由 PixcaseApp.Configuration.Provider 提供配置,
          显式移除默认的 WorkManagerInitializer(避免 androidx.startup 自动初始化)。
          lint "RemoveWorkManagerInitializer" 要求这个声明存在以证明有意禁用。
        -->
        <provider
            android:name="androidx.startup.InitializationProvider"
            android:authorities="${applicationId}.androidx-startup"
            android:exported="false"
            tools:node="merge">
            <meta-data
                android:name="androidx.work.WorkManagerInitializer"
                android:value="androidx.startup"
                tools:node="remove" />
        </provider>

        <meta-data
            android:name="android.max_aspect"
            android:value="2.4" />

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:configChanges="orientation|screenSize|keyboardHidden|screenLayout|smallestScreenSize|uiMode|density|fontScale">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

    </application>

</manifest>