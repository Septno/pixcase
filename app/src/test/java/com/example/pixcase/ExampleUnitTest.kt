package com.example.pixcase

import org.junit.Assert.assertEquals
import org.junit.Test

/**
 * 占位单元测试,验证 Gradle 测试 task 能跑起来。
 * 后续阶段按 ViewModel / Repository / DAO 维度补真实用例。
 */
class ExampleUnitTest {
    @Test
    fun addition_isCorrect() {
        assertEquals(4, 2 + 2)
    }
}
